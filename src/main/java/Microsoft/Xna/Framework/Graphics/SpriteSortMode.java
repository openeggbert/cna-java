package Microsoft.Xna.Framework.Graphics;

/** Controls the ordering and flushing policy for one SpriteBatch interval. */
public enum SpriteSortMode {
    Deferred,
    Immediate,
    Texture,
    BackToFront,
    FrontToBack
}
