package Microsoft.Xna.Framework.Graphics;

/** Comparison operations used by depth and stencil tests. */
public enum CompareFunction {
    Always,
    Never,
    Less,
    LessEqual,
    Equal,
    GreaterEqual,
    Greater,
    NotEqual
}
