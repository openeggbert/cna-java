package Microsoft.Xna.Framework;

import java.util.LinkedHashMap;

/** Ordered Java map projection of XNA launch key/value parameters. */
@SuppressWarnings("serial")
public class LaunchParameters extends LinkedHashMap<String, String> {

    public LaunchParameters() {
    }
}
