package Microsoft.Xna.Framework.Net;

import Microsoft.Xna.Framework.EventArgs;

/** Reports that the session's game has ended and it has returned to the lobby. */
public class GameEndedEventArgs extends EventArgs {

    public GameEndedEventArgs() {
    }
}
