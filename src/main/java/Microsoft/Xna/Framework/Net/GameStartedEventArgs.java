package Microsoft.Xna.Framework.Net;

import Microsoft.Xna.Framework.EventArgs;

/** Reports that the session's game has started. */
public class GameStartedEventArgs extends EventArgs {

    public GameStartedEventArgs() {
    }
}
